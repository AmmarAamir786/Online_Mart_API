syntax = "proto3";

enum OperationType {
  CREATE = 0;
  UPDATE = 1;
  DELETE = 2;
}

message Order {
  optional int32 id = 1;
  map<int32, int32> product_quantities = 2;  // Key: Product ID, Value: Quantity
  optional OperationType operation = 3;
}